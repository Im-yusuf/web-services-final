# Set this to your deployed Railway backend URL in Vercel's environment variables.
# Leave empty (or remove) for local development — the Vite proxy handles /api requests.
VITE_API_BASE_URL=https://your-backend.up.railway.app/api
